openssl_conf = openssl_init

[openssl_init]
providers = provider_sect

[provider_sect]
default      = default_sect
oqsprovider  = oqs_sect

[default_sect]
activate = 1

[oqs_sect]
module   = /usr/local/ssl/lib/ossl-modules/oqsprovider.so
activate = 1
